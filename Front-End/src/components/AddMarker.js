import React from "react";

import Button from "@material-ui/core/Button";

const AddMarker = ({ city, addmarker }) => {
  return <Button variant="outlined">Add a Marker</Button>;
};

export default AddMarker;
