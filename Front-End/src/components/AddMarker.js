import React from "react";

import Button from "@material-ui/core/Button";

const AddMarker = ({ city, addmarker }) => {
  return <Button onClick={"addmarker"}>Add a Marker</Button>;
};

export default AddMarker;
